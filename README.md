# LocaPin (Mobile Only)

LocaPin is now scoped as a **single Android mobile application**. The previous admin website assumption has been removed from the system scope.

- One app
- One shared login screen
- Role-based routing to Admin or Tourist dashboards inside the mobile app

For implementation and mock-auth details, see `mobile/README.md`.
